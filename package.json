{
  "name": "fvtt-pf2e-feat-filter",
  "version": "0.1.0",
  "scripts": {
    "build": "esbuild src/module.ts --bundle --keep-names --minify --sourcemap --drop-labels=DEV --outdir=scripts",
    "dev": "esbuild src/module.ts --bundle --keep-names --watch --sourcemap --outdir=scripts",
    "generate": "node build/generate-mapping.js"
  },
  "repository": {
    "type": "git"
  },
  "author": "kromko",
  "license": "MIT",
  "dependencies": {
    "esbuild": "^0.25.12",
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "@7h3laughingman/pf2e-types": "^8.0.2",
    "prettier": "3.5.3"
  },
  "type": "module"
}
